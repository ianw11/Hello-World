This directory started off with
$ git init

Then a file was created and added
$ git add README

And the staged files were committed
$ git commit -m 'first commit'

Then evolved with
$ git remote add origin 'git@github.com:ianw11/Hello-World.git'
$ git push origin master
and all was good
